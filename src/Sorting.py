def bubble(array):
	def _swap(x,y):
		tmp = array[x]
		array[x]=array[y]
		array[y]=tmp
		
		do:
			swapped=false
			for i in len(array[])
				if array[i] > array[i+1]
					_swap(array[i],array[i+1])
					return swapped=true
		while:
			swapped=true
    raise NotImplementedError

def merge():
    raise NotImplementedError

def count():
    raise NotImplementedError

def search(array):
	raise NotImplementedError
